---
apiVersion: "monitoring.coreos.com/v1"
kind: "PodMonitor"
metadata:
  name: "gotk"
spec:
  namespaceSelector:
    matchNames:
    - "flux-system"
  selector:
    matchLabels: {}
  podMetricsEndpoints:
  - port: "http-prom"
    path: "/metrics"
    interval: "30s"

